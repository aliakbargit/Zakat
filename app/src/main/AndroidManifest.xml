<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zakat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.admin.activities.ApplicationDetailsViewResult"></activity>
        <activity android:name=".views.auth.register.RegisterActivity" />
        <activity android:name=".views.admin.AdminHome" />
        <activity android:name=".views.user.UserHome" />
        <activity
            android:name=".views.auth.AuthActivity"
            android:theme="@style/AppAuthPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>